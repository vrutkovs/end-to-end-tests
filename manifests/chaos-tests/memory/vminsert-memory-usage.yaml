kind: StressChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: vminsert-memory-usage
spec:
  selector:
    namespaces:
      - vm
    labelSelectors:
      app.kubernetes.io/name: vminsert
  mode: all
  stressors:
    memory:
      size: 100%
      workers: 16
  duration: 10m
