kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: vmselect-to-vmstorage-packet-delay
spec:
  selector:
    namespaces:
      - vm
    labelSelectors:
      app.kubernetes.io/name: vmselect
  mode: all
  action: delay
  duration: 7m
  delay:
    latency: 30s
    correlation: '0'
    jitter: 5s
  direction: to
  target:
    selector:
      namespaces:
        - vm
      labelSelectors:
        app.kubernetes.io/name: vmstorage
    mode: all
