kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: vminsert-pod-failure
spec:
  selector:
    namespaces:
      - vm
    labelSelectors:
      app.kubernetes.io/name: vminsert
  mode: all
  action: pod-failure
  duration: 5m
