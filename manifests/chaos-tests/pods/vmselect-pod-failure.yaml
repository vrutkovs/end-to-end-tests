kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: vmselect-pod-failure
spec:
  selector:
    namespaces:
      - vm
    labelSelectors:
      app.kubernetes.io/name: vmselect
  mode: all
  action: pod-failure
  duration: 7m
