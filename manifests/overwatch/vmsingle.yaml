apiVersion: operator.victoriametrics.com/v1beta1
kind: VMSingle
metadata:
  name: overwatch
  namespace: vm
spec:
  storage:
    accessModes:
      - ReadWriteOnce
    resources:
      limits:
        memory: 3000Mi
      requests:
        memory: 500Mi
        storage: 50Gi
